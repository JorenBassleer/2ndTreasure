<?php
namespace App\Traits;
use Illuminate\Http\Request;
trait LeaderBoardsTrait {
    // protected function addToWeeklyLeaderBoard($weight, $user)
    // {
    //     dd(WeeklyLeaderBoard::where('amount_of_kg', '<', $weight)->orderBy('place', 'asc')->get());
    //     // Check if user is in leaderboards
    //     if(!$user->weeklyleaderboard > null) {
    //         // user is not in leaderboards yet
    //         // Check which place this user will come in
    //         $place = WeeklyLeaderBoard::where('amount_of_kg', '<', $weight)->orderBy('place', 'asc')->get();
    //     }
    //     WeeklyLeaderBoard::where('user_id');
    //     // is not check which place he should be
    //     // enter and increment all the places of rows
    //     // that have a lower value than user

    // }
}